# 2026-08-06 收尾验证 + 生产化加固（8.1.0）

## 上午：8.0 收尾
- 8.0 计划 16 步全部完成（详见 memory/2026-08-05.md 检查点）
- 修复：TaiyiChart 补 yue_gong/ri_gong（月计/日计落宫）进盘面；tests 清未用 import；新增 ruff.toml 固化基线
- 验证：pytest 121 passed；ruff 全绿；compileall 通过；离线排盘端到端 OK（10 时空盘 + 10 本命盘）

## 夜间：生产化加固 → 8.1.0（用户要求"生产环境可用"）
- 版本：APP_VERSION 8.0.0 → 8.1.0；新增 `version` 子命令（运维确认版本用）
- Docker：HEALTHCHECK 复用 status 命令（读 health.json，start_period 90s）；STOPSIGNAL SIGTERM；/config 目录；
  新增 .dockerignore（排除 runtime/tests/备份/凭据）
- deploy/ 资产：
  - docker-compose.example.yml：非 root、read_only 根文件系统、tmpfs /tmp、no-new-privileges、
    env_file 注入凭据、config.json/runtime 卷挂载、日志轮转（json-file 20m×5）、stop_grace_period 45s
  - gufa-quant-pro.env.example：GUFA_API_KEY/SECRET/PASSWORD + OPENAI_API_KEY 模板（对应 config 默认键名）
  - README.md：生产部署指南（两种路径、上线检查清单 8 项、升级/回滚、日常运维、安全清单、FAQ）
- systemd 服务示例（已有）与 Docker 双路径；README 主文件加「生产部署」章节
- git 初始化 + 首次提交：commit a958bed，34 个文件，无密钥/备份入库（*.env、*.bak-*、config.json、credentials*.json 均忽略）
- 验证：pytest 121 passed；ruff 全绿；`docker compose config --quiet` 通过；
  version/init-config/help 冒烟 OK；**Docker 引擎未运行**（Docker Desktop 已安装但 Linux 引擎 180s 未就绪）→ 镜像构建未验证，
  待用户启动 Docker Desktop 后执行 `docker build -t gufa-quant-pro:8.1.0 .`

## 遗留
- Docker 镜像实际构建与容器运行验证（需 Docker Desktop 引擎就绪）
- 真机联调：setup/env 填凭据 → validate → once → 观察期 → 正式盘 ack（按 deploy/README.md 清单）
